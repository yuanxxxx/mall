ext {
    mybatisGeneratorVersion = '1.3.7'
}

dependencies {
    compile group: 'org.mybatis.generator', name: 'mybatis-generator-core', version: "${mybatisGeneratorVersion}"
    compile project(":mall-service-util")
}